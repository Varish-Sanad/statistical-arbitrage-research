[project]
name = "statistical-arbitrage-research"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pandas", "numpy", "statsmodels", "yfinance"]

[tool.setuptools.packages.find]
include = ["statarb*"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
